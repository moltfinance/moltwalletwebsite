the official moltwallet website!

Key points to communicate:
- Security: private keys are stored client-side; we never touch them.
- Features: Tags (username → wallet), token deploy, swaps, transfers.
- Vision: agents as real economic actors — programmable cooperation that can change finance forever.

